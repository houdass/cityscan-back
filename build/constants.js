'use strict';

var ROLES = {
  CLIENT: 'CLIENT',
  ADMIN: 'ADMIN'
};

var PERMISSIONS = {
  READ_USERS: 'READ_USERS',
  WRITE_USERS: 'WRITE_USERS'
};

module.exports = {
  ROLES: ROLES,
  PERMISSIONS: PERMISSIONS
};